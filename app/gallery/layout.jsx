export const metadata = {
  title: "Kappa Theta Pi UTD - Gallery",
  description: "A page with pictures of our times together",
};

export default function GalleryLayout({ children }) {
  return children;
}
